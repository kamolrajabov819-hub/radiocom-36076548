node_modules
dist
.output
.netlify
.vinxi
pnpm-lock.yaml
package-lock.json
bun.lock

# Installed skill and agent packages, vendored in and authored elsewhere. Same
# reasoning as node_modules: we do not own the formatting, so checking it only
# produces noise nobody may act on. Kept in step with `eslint.config.js`.
.claude/

# Scratch, untracked: QA script output and one-off measurement scripts from
# earlier phases. Kept in step with `eslint.config.js`.
.qa-tmp/
scripts/.tmp/

# Generated, and not by us. `routeTree.gen.ts` is TanStack Router's output; the
# two Supabase files are written by Lovable — `previewAuthStorage.ts` says so in
# its first line, "This file is automatically generated. Do not edit it
# directly." Formatting them would be undone by the next sync and would put this
# repo in conflict with the editor that owns them.
routeTree.gen.ts
src/integrations/supabase/types.ts
src/integrations/supabase/previewAuthStorage.ts
